# OpenAPI

Run the API and open http://127.0.0.1:8011/docs

Key routes: `/projects`, `/documents/upload`, `/authoring/draft`, `/status/{thread_id}`, `/drafts/{id}`, `/drafts/{id}/claims`, `/drafts/{id}/evidence`, `/drafts/{id}/provenance`, `/approve/{thread_id}`, `/audit/{request_id}`, `/health`, `/ready`, `/metrics`.
