# Knowledge graph seeds

JSON graphs under this directory are loaded with idempotent Neo4j `MERGE`, or used as an in-memory fallback when Neo4j is unavailable.

Generate with `scripts/generate_synthetic_bank_data.py`.
