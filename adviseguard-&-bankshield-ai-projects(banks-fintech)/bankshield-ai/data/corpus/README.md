# Corpus

NDA-safe synthetic AML/OFAC/playbook/case documents generated by:

```bash
uv run python -m scripts.generate_synthetic_bank_data
```

Domains: `aml_policy`, `ofac_guidance`, `fraud_playbooks`, `kyc_profiles`, `closed_cases`, `sar_examples`.
